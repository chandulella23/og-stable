import { Routes, RouterModule } from '@angular/router';
import { IndexWhyComponent } from './index-why/index-why.component';

export const routes: Routes = [
  
  
  { path: '', component: IndexWhyComponent },
];