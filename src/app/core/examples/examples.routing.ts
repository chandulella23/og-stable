import { Routes, RouterModule } from '@angular/router';
import { IndexExamplesComponent } from './index-examples/index-examples.component';

export const routes: Routes = [
  
  
  { path: '', component: IndexExamplesComponent },
];