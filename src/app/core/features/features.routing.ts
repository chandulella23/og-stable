import { Routes, RouterModule } from '@angular/router';
import { IndexFeaturesComponent } from './index-features/index-features.component';

export const routes: Routes = [
  
  
  { path: '', component: IndexFeaturesComponent },
];