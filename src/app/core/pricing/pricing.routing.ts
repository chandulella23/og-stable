import { Routes, RouterModule } from '@angular/router';
import { IndexPricingComponent } from './index-pricing/index-pricing.component';

export const routes: Routes = [
  
  
  { path: '', component: IndexPricingComponent },
];